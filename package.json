{
  "name": "dotforge-web",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "type-check": "npm run generate-types && tsc",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.0.1",
    "@astrojs/preact": "^6.0.0",
    "@dotforge/core": "^0.8.0",
    "astro": "^7.0.3",
    "lucide-preact": "^1.21.0",
    "preact": "^10.29.2"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "@playwright/test": "^1.61.0",
    "@types/node": "^25.9.3",
    "wrangler": "^4.95.0"
  },
  "engines": {
    "node": ">=24.12.0"
  }
}
